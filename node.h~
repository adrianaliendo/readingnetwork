/* 
 * File:   node.h
 * Author: aliendo
 *
 * Created on 11 de diciembre de 2013, 09:59 AM
 */

#ifndef NODE_H
#define	NODE_H
#include <iostream>
#include <string>
#include <stdlib.h>
#include <mpi.h>
#include "omp.h"
using namespace std;


class node {
public:
    int idnode;
    char namenode[MPI_MAX_PROCESSOR_NAME];
    int numprocs;
    int nthread;
    int threads[5];//Deberia ser numprocs
    string names[5];//Deberia ser numprocs 

    node();
    void setIdnode();
    void setNamenode();
    void setNumnode();
    void setthread();

    //Para descubrimiento de la clase
    int getnatr();
    string gettypeatr(int n);
    string getnameatr(int n);
private:
    int large_name;

    int natr;
    string typeatr[7];
    string nameatr[7];
    void setnatr();
    void settypeatr();
    void setnameatr();

    /*node(const node& orig);
    virtual ~node();*/

};

#endif	/* NODE_H */

